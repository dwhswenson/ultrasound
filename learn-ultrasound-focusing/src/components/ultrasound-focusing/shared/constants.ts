/**
 * Shared constants for the ultrasound focusing application.
 * This file centralizes important values used across multiple components.
 */

/** Visual speed of sound waves in pixels per second. */
export const VISUAL_SPEED_PX_PER_SEC = 150;

/** Application frame rate in frames per second. */
export const APP_FRAME_RATE = 30;

/** Default element radius in pixels. */
export const DEFAULT_ELEMENT_RADIUS = 10;

/** Default element line length in pixels. */
export const DEFAULT_LINE_LENGTH = 200;

/**
 * Default element horizontal position as a fraction of canvas width.
 * 0.4 means elements are positioned at 40% from the left edge.
 */
export const DEFAULT_ELEMENT_X_POSITION = 0.4;
